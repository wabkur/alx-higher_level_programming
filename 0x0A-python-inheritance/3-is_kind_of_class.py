#!/usr/bin/python3
"""contain the first go at it instance"""

def is_kind_of_class(obj, a_class):
    """True if obj is an instance or inherited from a_class, else false """
    return isintance(obj, a_class)
